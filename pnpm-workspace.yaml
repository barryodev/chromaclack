packages:
  - 'apps/*'
  - 'packages/*'

# pnpm blocks lifecycle scripts unless a package is explicitly allowed.
# These need theirs to fetch or patch native binaries.
onlyBuiltDependencies:
  - '@tauri-apps/cli'
  - esbuild
