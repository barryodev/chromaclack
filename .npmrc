# Tauri's CLI and Vite's esbuild resolve better with a flat-ish store here.
auto-install-peers=true
strict-peer-dependencies=false
# SvelteKit reads package internals of linked workspace deps.
shamefully-hoist=false
