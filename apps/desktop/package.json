{
  "name": "@repo/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "android:init": "tauri android init",
    "android:dev": "tauri android dev --host $(ip route get 1.1.1.1 | awk '{print $7; exit}')",
    "android:build": "tauri android build",
    "icons": "tauri icon ./src-tauri/icons/icon.png"
  },
  "devDependencies": {
    "@repo/frontend": "workspace:*",
    "@tauri-apps/cli": "^2.11.4"
  }
}
